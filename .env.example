# =========================================================================
# FRONTEND CONFIGURATIONS (Vercel Deployable)
# =========================================================================
NEXT_PUBLIC_API_URL=http://localhost:5000/api
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# =========================================================================
# BACKEND CONFIGURATIONS (Render Deployable)
# =========================================================================
PORT=5000
DATABASE_URL=postgresql://postgres:password123@localhost:5432/hd_tech_solutions?sslmode=disable
JWT_SECRET=production_ready_secret_key_minimum_32_chars_long
JWT_EXPIRES_IN=7d
CLIENT_URL=http://localhost:3000

# Cloudinary Storage Credentials (Optional, streams default to local storage if empty)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# SMTP Email Configurations (Optional)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
